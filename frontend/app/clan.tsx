import { View, Text } from "react-native";

export default function ClanScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text style={{ color: "#fff" }}>Клан</Text>
    </View>
  );
}
